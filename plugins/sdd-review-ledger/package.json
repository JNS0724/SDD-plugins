{
  "name": "sdd-review-ledger",
  "private": true,
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "test:unit": "node --test \"test/unit/*.test.js\"",
    "build": "node build.mjs",
    "build:check": "node build.mjs --check"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
